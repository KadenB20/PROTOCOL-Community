name: Report a problem
description: Something did not work as expected.
body:
  - type: markdown
    attributes:
      value: "Keep personal records private. Do not attach databases, backups, bloodwork, or private photos."
  - type: input
    id: version
    attributes:
      label: PROTOCOL version
      description: Shown in Settings → Workspace & Data → About PROTOCOL.
    validations:
      required: true
  - type: input
    id: windows
    attributes:
      label: Windows version
    validations:
      required: true
  - type: textarea
    id: problem
    attributes:
      label: What happened
      description: What were you trying to do, and what happened instead?
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: Steps to repeat the problem
    validations:
      required: true
